import SwiftUI

public extension Color {
    // Grayscale Colors
    static let dividerGray = Color(red: 0.9, green: 0.9, blue: 0.9)
    static let backgroundGray = Color(red: 0.95, green: 0.95, blue: 0.95)
    
    // Theme Colors
    static let gradeLight = Color(red: 0.8, green: 0.9, blue: 1.0)  // Light blue for grades
    static let heroLight = Color(red: 1.0, green: 0.9, blue: 0.8)   // Light orange for hero elements
    
    // Additional theme colors can be added here
    static let primary = Color(red: 0.2, green: 0.4, blue: 0.8)     // Primary brand color
    static let secondary = Color(red: 0.8, green: 0.4, blue: 0.2)   // Secondary brand color
    static let accent = Color(red: 0.4, green: 0.8, blue: 0.2)      // Accent color
} 